# 实验一后续执行路线

## 先理解老师到底检查什么

课件是课程背景和总体要求；你补充的四条是现场检查时必须主动展示的证据。两者合并后，现场演示应该是一条完整链路：

`摄像头/视频 → 检测模型 → 框/类别/置信度 → 当前目标数量和 FPS → ROS2 消息 → 独立测试统计`

老师可能会追问“为什么这个准确率可信”“FPS 怎么测”“这段代码是不是自己写的”，所以不能只准备一个能弹窗的 demo。

## 建议的最低可交付版本

### 数据
- 两类：`mouse` + 一个第二类桌面物体。水杯或键盘通常比外观相似的小物体更容易在短周期内达到 80%。
- 每类至少准备约 250 到 400 张有变化的标注图作为起步目标；当前鼠标 200 张需要检查是否有足够的视角和背景变化。
- 采集训练、验证、测试三批数据时改变拍摄时间、位置、距离、角度、光照和遮挡。

### 标注与划分
- 每个目标一个矩形框，类别名称固定且全程一致，不要一会儿用中文一会儿用英文。
- `train/val/test` 按视频或拍摄批次划分，而不是随机抽相邻帧。
- 标注完成后随机抽查至少 20 张：漏框、框过大、框偏移、类别写错都要修正。

### 模型与评测
- 先训练轻量模型得到基线；保存训练参数、类别顺序、输入分辨率、权重文件和日志。
- 置信度阈值只在验证集上确定，测试集只运行一次并锁定规则。
- 评测脚本输出：总测试数、正确数、准确率、每类正确数、漏检数、误检数、混淆矩阵/错误案例路径。

### Jetson 与显示
- 先用 Python/OpenCV 完成实时画面，再移植 Jetson。
- 在画面上固定显示 `FPS`、`Objects`、各类别数量和每个框的置信度。
- FPS 用连续运行一段时间后的实际处理帧数计算：`FPS = 处理帧数 / 总耗时`；记录预热后的平均值，不能只截取一个瞬时峰值。
- 如果低于 5 FPS，按顺序尝试：降低输入分辨率、选择更轻模型、减少不必要的图像复制、启用 Jetson 的合适推理后端，再重新测量。

### ROS2
- 将推理器封装成独立类，ROS2 节点只负责订阅图像、调用推理器和发布消息。
- 话题至少记录：名称、消息类型、时间戳、类别 ID/名称、置信度、检测框坐标。
- 现场用 `ros2 topic list` 和 `ros2 topic echo` 证明消息确实在发布，不要只展示窗口。

## 代码讲解准备

你需要能按自己的代码回答以下问题：

1. 模型权重从哪里加载，类别 ID 如何映射到类别名？
2. 输入图像如何预处理，输出框如何还原到原图坐标？
3. 置信度阈值和 NMS 做什么，为什么选这个阈值？
4. 当前 `Objects` 数量是如何统计的，是否包含低置信度框？
5. FPS 是瞬时值还是滑动平均，分母和分子如何得到？
6. 准确率的真实框与预测框如何匹配，IoU 阈值是多少？
7. ROS2 消息字段分别代表什么，节点异常时如何退出？

同组不能共用一份代码时，保持自己的目录结构、函数命名、配置文件、评测实现和日志格式，并保留个人 commit。不要通过变量改名制造“不同代码”；真正独立的实现应该能独立解释和修改。

## GitHub/Gitee 提交流程

这不是可选项，而是评分注意事项的一部分。建议从现在开始建立个人仓库，至少按以下节点提交：

1. `init`: 项目目录、README、任务计划和环境说明。
2. `data-prep`: 抽帧脚本、数据集目录说明和采集记录；不要把过大的原始视频直接提交到仓库。
3. `labeling`: 数据集 YAML、类别定义和标注检查记录。
4. `training`: 训练脚本、配置、日志摘要和权重下载/存放说明。
5. `evaluation`: 20+ 测试样本清单、准确率脚本、结果表和错误案例。
6. `jetson-ros2`: Jetson 运行代码、FPS 日志、ROS2 节点和 topic 说明。
7. `report`: LaTeX 源码、编译后的 PDF、结果图表和运行说明。

最终报告中至少放入：个人仓库链接、commit 时间线截图、关键 commit 截图和仓库目录截图。代码、报告、模型说明应由个人仓库记录支撑；不要只在课程平台上传一个压缩包。

## 推荐时间安排

- 第 1 天：抽取鼠标帧、选第二类物体、完成三批采集计划。
- 第 2 到 3 天：标注、抽查、按视频划分数据集。
- 第 4 天：PC 训练基线，保存日志和最佳权重。
- 第 5 天：写离线评测，完成 20+ 测试样本和准确率表。
- 第 6 天：实时显示、FPS 测量、结果视频。
- 第 7 天：Jetson 部署和性能优化。
- 第 8 天：ROS2 发布验证、错误案例、运行说明和报告。

## 现在马上做的三件事

1. 运行已有抽帧脚本并人工查看鼠标图像质量。
2. 选定第二类物体并重新采集至少 3 段有差异的视频。
3. 不要急着训练：先确定类别名、数据集划分规则和 20 个独立测试样本编号。
