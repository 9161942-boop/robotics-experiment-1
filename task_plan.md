# 实验一：目标检测与识别任务计划

## 目标
完成“鼠标、笔记本电脑、水杯、手机”四类自采集目标检测系统，在 Jetson 上达到实时显示、平均不低于 5 FPS、20 个以上独立测试物体准确率不低于 80%，并通过 ROS2 发布检测结果。

## 验收口径
- [x] 四类物体：鼠标、笔记本电脑、水杯、手机；类别名称已固定为 `mouse`、`laptop`、`cup`、`phone`。
- [ ] 实时画面显示：类别、检测框、置信度、当前检测数量、实时 FPS。
- [ ] 测试集至少 20 个独立测试样本，建议四类各 5 个不同实物；测试图像/视频不能参与训练。
- [ ] 准确率 >= 80%，使用预先写明的逐物体判定规则和脚本计算，不能只凭肉眼估计。
- [ ] Jetson 上连续运行测试，平均 FPS >= 5；记录分辨率、模型、推理后端、平均 FPS 和最低/稳定 FPS。
- [ ] ROS2 发布检测结果，优先使用 `vision_msgs/Detection2DArray`，记录 topic 名称和消息字段。
- [ ] 保存结果视频、评测表、混淆/错误案例、运行说明、模型和代码。
- [ ] 使用个人 GitHub/Gitee 仓库提交代码、报告和练习，保留连续 commit；在实验报告中写入仓库链接并附 commit 记录截图。
- [ ] 报告使用自己设计的 LaTeX 模板，不能直接套用同组成员的报告模板。

## 阶段
- [x] 阶段 1：规划与数据准备方案
- [x] 阶段 2：目标框标注与质量检查（自动检查通过）
- [x] 阶段 2.1：新增 cup2/cup3/cup4 图片标注与质量检查
- [x] 阶段 3：模型训练、验证和版本记录（v2 已完成，最佳权重与配置已留档）
- [ ] 阶段 4：Jetson 部署、FPS 测量、实时显示
- [ ] 阶段 5：ROS2 节点与接口验证
- [ ] 阶段 6：20 个独立实物测试、准确率计算和错误案例整理
- [ ] 阶段 7：错误案例、视频、报告和答辩讲解

## 当前状态
**阶段 2 的标注与质量检查已完成。** 当前四类图片与 ISAT JSON 一一对应：cup 257、laptop 119、mouse 103、phone 116，共 595 张；全量 JSON 可解析，目标类别均在统一类别表内，图像尺寸一致，分割点均在图像范围内，无空标注。`cup_2.mp4` 和 `cup_3.mp4` 的筛选结果已保留 110 张，`cup_4.mp4` 的筛选结果已保留 69 张，删除的帧均未纳入数据集。

**阶段 2.5 数据转换已完成。** ISAT 多边形已转换为统一四类 YOLO 检测标签，生成 421/87/87 张 train/val/test 图像和 `manifest.csv`；所有标签均通过数值范围和图像/标签配对检查。根据抽帧编号区间恢复出 19 个实物/视频来源组，每个来源组都同时提供 train/val/test 连续时间块，因此不同外观均进入训练。转换脚本会去掉同一图像中 IoU >= 0.995 的同类重复框。

**阶段 3 v1/v2 PC 训练已完成。** v1 仅保留作过程证据；v2 使用修正后的 19 个来源组重新训练，预训练 YOLO11n、640 输入、batch 16、80 epochs、seed 42、RTX 4060 CUDA。v2 最佳验证行（epoch 63）为 precision=0.99337、recall=0.99405、mAP50=0.99064、mAP50-95=0.98244；帧级测试为 103/104=99.04%。最终课程准确率仍需独立 20 个实物测试。

## 下一步顺序
1. 将 ISAT JSON 转成 YOLO 检测标签 TXT，并自动检查类别、bbox、图片尺寸和标签数量。
2. 为四类建立统一类别顺序：`mouse`、`laptop`、`cup`、`phone`。
3. 完成新增 cup2/cup3/cup4 图片标注后，按拍摄视频/场景划分 train、val、test，不要随机打散相邻帧；cup 现在已有 4 个视频，可用于场景隔离划分。
4. 先在 PC 上训练轻量级模型得到基线，保存训练配置、日志、权重和指标。v2 已完成，当前权重为 `runs/yolo11n_4class_v2/weights/best.pt`。
5. 将 `best.pt` 和实时检测程序部署到 Jetson，先验证摄像头和 CUDA 推理，再在 Jetson 上导出 TensorRT FP16 引擎并测量平均 FPS。
6. 加入 ROS2 节点，记录 topic、消息类型、时间戳和坐标约定。
7. 使用四类各 5 个、共 20 个未参与训练的实物完成最终准确率测试，保存判定表和典型错误案例。
8. 录制带 FPS 和检测数量叠加的结果视频，完成报告与代码讲解材料。
9. 按课程要求整理个人 GitHub/Gitee 仓库，提交转换脚本、训练代码、运行说明和结果；截取仓库链接、提交历史和关键 commit，写入报告。

## 关键问题
1. 第二类物体最终选择什么，是否能采集至少 200 到 300 张有差异的图像？
2. Jetson 型号、JetPack/ROS2 版本和摄像头输入方式是什么？这些决定部署后端和安装方式。
3. 测试的 20 个物体能否使用训练时没有出现过的实物、位置或场景？

## 个人代码要求
- [ ] 不复制同组成员的完整代码；从目录结构、配置方式、节点接口和评测脚本设计上保持独立。
- [ ] 每个模块自己能解释：数据读取、模型推理、NMS/阈值、FPS 计算、匹配规则、ROS2 发布。
- [ ] 使用个人 Git 仓库和连续 commit，保留训练配置、模型版本、测试结果和修改原因。
- [ ] 报告中画出自己的数据流：摄像头 → 推理 → 后处理 → 显示/FPS → ROS2。

## 风险与处理
- 相邻视频帧高度相似：按视频或拍摄批次划分数据，避免虚高指标。
- 只有 200 张鼠标图：补充不同背景、姿态、距离和遮挡，必要时增加数据增强。
- FPS 不足：降低输入分辨率、使用轻量模型、TensorRT FP16，并记录优化前后数据。
- 准确率不足：先检查标注和测试规则，再增加困难样本，不能通过调低置信度掩盖误检。

## 执行中遇到的问题
- Ultralytics 会把 `data.yaml` 中的 `path: .` 按运行上下文解析，首次启动时找不到 `images/val`；现改为 `path: dataset/yolo`，并由 `train_yolo.py` 自动切换到项目根目录，兼顾可移植性与稳定解析。
